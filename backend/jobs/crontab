# [UNCONFIRMED: D6] supercronic schedule for the `fetcher` compose service.
# Once a day at 06:00 UTC. Runs in /app with the service's environment; a run that overlaps
# another exits early.
0 6 * * * python -m src.fetcher
